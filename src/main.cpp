#include "Sistema.h"

int main() {                    // Função principal que serve como ponto de entrada do programa.
    Sistema sistema;            // Cria uma instância do objeto Sistema.
    sistema.run();              // Chama o método run() da instância Sistema para executar o sistema.
    return 0;                   // Retorna 0 para indicar que o programa foi executado com sucesso.
}
